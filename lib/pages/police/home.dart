import 'package:flutter/material.dart';

class PoliceHome extends StatelessWidget {
  const PoliceHome({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text("Police Home")));
}